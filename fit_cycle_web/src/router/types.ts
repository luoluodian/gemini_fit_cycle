/**
 * 路由类型定义
 * 
 * 注意：这些类型定义已不再使用，保留仅用于未来可能的扩展需求
 * 当前路由系统已简化为函数式API，不再需要这些选项接口
 */

// 如果需要支持更复杂的路由选项（如events），可以重新启用这些类型
// export interface NavigateOptions {
//   url: string;
//   params?: Record<string, any>;
//   events?: Record<string, Function>;
// }
