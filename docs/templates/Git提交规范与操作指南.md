# Git提交规范与操作指南

> **版本**: v1.0
> **日期**: 2026-01-29
> **适用**: T9功能交付阶段

## 🎯 为什么需要Git提交

### 问题背景

在之前的流程中，T9阶段虽然有"执行Git提交"的清单项，但存在以下问题：

1. ❌ 只有清单项，没有具体执行命令
2. ❌ AI可能只勾选清单，实际未执行git命令
3. ❌ 缺少提交验证机制
4. ❌ 没有详细的操作步骤

### 解决方案

✅ 详细的Git操作步骤
✅ 强制验证提交成功
✅ 标准化的commit message格式
✅ 常见问题处理方案

---

## 📋 T9阶段Git提交完整流程

### 第1步: 验收确认（5分钟）

在提交代码前，必须确认所有质量门禁已通过：

```markdown
✅ T5 分析文档已生成
✅ T6 变更清单已生成
✅ T7 测试报告已生成（所有测试通过）
✅ T7.5 设计走查已通过（前端任务，一致性≥95%）
✅ T8 审计报告已生成（无严重问题）
✅ T8.5 审查意见为 Pass
```

**如果任何一项未通过，不得提交代码！**

---

### 第2步: 检查工作区状态（2分钟）

```bash
# 1. 查看当前分支
git branch
# 输出示例：
# * main
#   feature/auth-login

# 2. 查看工作区状态
git status
# 输出示例：
# On branch main
# Changes not staged for commit:
#   modified:   src/modules/auth/auth.controller.ts
#   modified:   src/modules/auth/auth.service.ts
# Untracked files:
#   src/modules/auth/dto/login.dto.ts

# 3. 查看具体修改内容
git diff
# 输出：显示文件的具体修改

# 4. 查看未跟踪的文件
git ls-files --others --exclude-standard
```

**检查要点**：
- ✅ 确认所有修改的文件都是本任务相关的
- ✅ 确认没有遗漏的文件
- ❌ 确认没有临时文件（*.log, *.tmp, node_modules/等）
- ❌ 确认没有敏感信息（密码、密钥等）

---

### 第3步: 生成Commit Message（3分钟）

#### 格式规范

```
<type>(<scope>): <subject> (#任务ID)

[可选的详细描述]
```

#### Type类型

| Type | 说明 | 使用场景 |
|---|---|---|
| `feat` | 新功能 | 实现新的API、新的UI组件 |
| `fix` | 修复bug | 修复功能bug、修复样式问题 |
| `refactor` | 重构 | 代码重构，不改变功能 |
| `style` | 样式 | UI样式调整、代码格式化 |
| `perf` | 性能优化 | 提升性能的代码修改 |
| `test` | 测试 | 添加测试、联调测试 |
| `docs` | 文档 | 文档更新 |
| `chore` | 构建/工具 | 构建配置、依赖更新 |

#### Scope范围

| Scope | 说明 |
|---|---|
| `auth` | 认证相关 |
| `user` | 用户相关 |
| `food` | 食物相关 |
| `diet-log` | 饮食记录相关 |
| `diet-plan` | 饮食计划相关 |
| `analytics` | 分析统计相关 |
| `common` | 通用组件/工具 |
| `ui` | UI相关 |

#### 示例

**后端任务**：
```bash
feat(auth): 实现微信登录Controller层 (#U-1.1)
feat(auth): 实现微信登录Service层和JWT生成 (#U-1.2)
feat(food): 实现食物列表分页查询接口 (#F-1.1)
feat(diet-log): 实现添加饮食记录接口 (#L-2.1)
```

**前端任务**：
```bash
feat(auth): 封装前端认证服务API层 (#U-2.1)
feat(auth): 实现登录页面UI结构 (#U-3.1)
style(auth): 实现登录页面样式和动画效果 (#U-3.2)
feat(food): 实现食物卡片组件 (#F-8.1)
```

**UI还原任务**：
```bash
style(auth): 登录页面UI还原度验证通过 (#U-4.3)
style(food): 食物库页面UI还原度验证通过 (#F-7.5)
```

**联调任务**：
```bash
test(auth): 完成登录接口前后端联调 (#U-2.3)
test(food): 完成食物接口联调测试 (#F-10.3)
```

**修复bug**：
```bash
fix(auth): 修复token过期未正确跳转登录页 (#U-5.1)
fix(food): 修复食物搜索结果为空时的显示问题 (#F-7.2)
```

---

### 第4步: 添加文件到暂存区（2分钟）

#### 方式1: 添加所有修改（推荐）

适用于单任务开发，确保所有修改都是本任务相关的。

```bash
git add .
```

#### 方式2: 添加特定文件

适用于工作区有多个任务的修改，只提交当前任务相关的文件。

```bash
# 添加单个文件
git add src/modules/auth/auth.controller.ts

# 添加多个文件
git add src/modules/auth/auth.controller.ts \
        src/modules/auth/auth.service.ts \
        src/modules/auth/dto/login.dto.ts

# 添加整个目录
git add src/modules/auth/
```

#### 方式3: 交互式添加

适用于需要精细控制哪些修改要提交。

```bash
git add -p

# 会逐个显示修改，询问是否添加：
# y - 添加这个修改
# n - 不添加这个修改
# s - 拆分成更小的块
# q - 退出
```

#### 验证添加结果

```bash
# 查看暂存区状态
git status

# 输出示例：
# On branch main
# Changes to be committed:
#   modified:   src/modules/auth/auth.controller.ts
#   modified:   src/modules/auth/auth.service.ts
#   new file:   src/modules/auth/dto/login.dto.ts

# 查看暂存区的具体内容
git diff --cached
```

---

### 第5步: 提交到本地仓库（2分钟）

#### 基本提交

```bash
git commit -m "feat(auth): 实现微信登录Controller层 (#U-1.1)"
```

#### 带详细描述的提交

```bash
git commit -m "feat(auth): 实现微信登录Controller层 (#U-1.1)" -m "
- 创建AuthController
- 实现/auth/login路由
- 集成微信OpenID验证
- 返回JWT token
- 添加单元测试
"
```

#### 提交成功的输出

```bash
[main 1a2b3c4] feat(auth): 实现微信登录Controller层 (#U-1.1)
 3 files changed, 150 insertions(+), 5 deletions(-)
 create mode 100644 src/modules/auth/dto/login.dto.ts
```

---

### 第6步: 验证提交成功（2分钟）

#### 查看最新commit

```bash
git log -1

# 输出示例：
# commit 1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0
# Author: Your Name <your.email@example.com>
# Date:   Wed Jan 29 15:30:00 2026 +0800
#
#     feat(auth): 实现微信登录Controller层 (#U-1.1)
```

#### 查看commit详情

```bash
git show HEAD

# 输出：显示commit的详细信息和修改内容
```

#### 查看最近的commits

```bash
git log --oneline -5

# 输出示例：
# 1a2b3c4 feat(auth): 实现微信登录Controller层 (#U-1.1)
# 9s8r7q6 feat(user): 设计User数据库表 (#U-9)
# 5p4o3n2 chore: 初始化项目结构
```

#### 验证文件是否已提交

```bash
# 查看工作区状态，应该是干净的
git status

# 输出应该是：
# On branch main
# nothing to commit, working tree clean
```

---

### 第7步: 推送到远程仓库（3分钟）

> **注意**: 根据团队规范决定是否需要推送

#### 推送到main分支

```bash
git push origin main

# 输出示例：
# Enumerating objects: 10, done.
# Counting objects: 100% (10/10), done.
# Delta compression using up to 8 threads
# Compressing objects: 100% (6/6), done.
# Writing objects: 100% (6/6), 1.5 KiB | 1.5 MiB/s, done.
# Total 6 (delta 4), reused 0 (delta 0)
# To https://github.com/username/repo.git
#    9s8r7q6..1a2b3c4  main -> main
```

#### 首次推送（设置上游分支）

```bash
git push -u origin main

# -u 参数会设置上游分支，之后可以直接使用 git push
```

#### 推送到功能分支

```bash
# 如果在功能分支上开发
git push origin feature/auth-login
```

#### 验证推送成功

```bash
# 查看远程分支最新commit
git log origin/main -1

# 查看本地和远程的差异（应该没有差异）
git log origin/main..HEAD

# 如果输出为空，说明本地和远程已同步
```

---

### 第8步: 更新看板和归档（5分钟）

#### 更新看板

```bash
# 1. 更新任务看板
# 编辑 docs/pj_docs/04.5_原子任务交付看板.md
# 将任务的 T9 列标记为 ✅
# 将任务状态更新为"已完成"

# 2. 记录Git信息
# 在任务看板中记录：
# - Git commit hash: 1a2b3c4
# - Git commit message: feat(auth): 实现微信登录Controller层 (#U-1.1)
# - 提交时间: 2026-01-29 15:30:00
```

#### 归档文档

```bash
# 1. 创建任务目录
mkdir -p docs/pj_docs/tasks/U-1.1

# 2. 移动文档
mv T5_分析文档.md docs/pj_docs/tasks/U-1.1/
mv T6_变更清单.md docs/pj_docs/tasks/U-1.1/
mv T7_测试报告.md docs/pj_docs/tasks/U-1.1/
mv T8_审计报告.md docs/pj_docs/tasks/U-1.1/
mv T8.5_审查意见.md docs/pj_docs/tasks/U-1.1/

# 3. 创建任务README
cat > docs/pj_docs/tasks/U-1.1/README.md << 'EOF'
# U-1.1: 微信登录Controller层

## 任务信息

- **任务ID**: U-1.1
- **任务名称**: 实现微信登录接口 - Controller层
- **优先级**: P0
- **任务类型**: 🔧 后端
- **预估工时**: 1h
- **实际工时**: 1.2h
- **状态**: ✅ 已完成

## Git信息

- **Commit Hash**: 1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0
- **Commit Message**: feat(auth): 实现微信登录Controller层 (#U-1.1)
- **提交时间**: 2026-01-29 15:30:00
- **提交人**: Your Name

## 文档清单

- [T5_分析文档.md](./T5_分析文档.md)
- [T6_变更清单.md](./T6_变更清单.md)
- [T7_测试报告.md](./T7_测试报告.md)
- [T8_审计报告.md](./T8_审计报告.md)
- [T8.5_审查意见.md](./T8.5_审查意见.md)

## 主要变更

- 创建 `src/modules/auth/auth.controller.ts`
- 实现 `/auth/login` 路由
- 集成微信OpenID验证
- 返回JWT token

## 测试结果

- 单元测试: ✅ 通过
- 接口测试: ✅ 通过
- 性能测试: ✅ 响应时间<100ms

## 审查结论

- T8 审计: ✅ 通过
- T8.5 审查: ✅ Pass
EOF
```

---

## 🚨 常见问题与解决方案

### 问题1: 提交了错误的文件

**场景**: 不小心把临时文件或敏感信息提交了

**解决方案**:

```bash
# 方法1: 撤销最后一次commit，保留修改
git reset --soft HEAD~1

# 重新添加正确的文件
git add <correct-files>
git commit -m "correct message"

# 方法2: 从暂存区移除特定文件
git reset HEAD <file-to-remove>
git commit -m "correct message"
```

---

### 问题2: Commit Message写错了

**场景**: 提交后发现commit message有错别字或格式不对

**解决方案**:

```bash
# 修改最后一次commit message
git commit --amend -m "correct message"

# 如果已经推送到远程，需要强制推送（谨慎使用）
git push --force origin main
```

---

### 问题3: 忘记添加某个文件

**场景**: 提交后发现遗漏了某个文件

**解决方案**:

```bash
# 添加遗漏的文件
git add <forgotten-file>

# 追加到最后一次commit（不改变commit message）
git commit --amend --no-edit

# 如果已经推送，需要强制推送
git push --force origin main
```

---

### 问题4: 需要撤销提交

**场景**: 发现提交的代码有严重问题，需要撤销

**解决方案**:

```bash
# 方法1: 撤销commit，保留修改（推荐）
git reset --soft HEAD~1
# 修改代码后重新提交

# 方法2: 撤销commit，丢弃修改（危险）
git reset --hard HEAD~1

# 方法3: 创建一个反向commit（推荐用于已推送的commit）
git revert HEAD
```

---

### 问题5: 需要合并多个commit

**场景**: 一个任务提交了多次，想合并成一个commit

**解决方案**:

```bash
# 交互式rebase最近3个commit
git rebase -i HEAD~3

# 在编辑器中：
# pick 1a2b3c4 第一个commit
# squash 5d6e7f8 第二个commit（合并到第一个）
# squash 9g0h1i2 第三个commit（合并到第一个）

# 保存后会提示编辑合并后的commit message
```

---

### 问题6: 推送失败（远程有更新）

**场景**: 推送时提示远程仓库有新的提交

**解决方案**:

```bash
# 方法1: 拉取并合并
git pull origin main
# 解决冲突（如果有）
git push origin main

# 方法2: 拉取并rebase（推荐）
git pull --rebase origin main
# 解决冲突（如果有）
git push origin main
```

---

### 问题7: 不小心提交到错误的分支

**场景**: 在main分支上开发，应该在功能分支上

**解决方案**:

```bash
# 1. 创建功能分支（保留当前commit）
git branch feature/auth-login

# 2. 切换到main分支
git checkout main

# 3. 撤销main分支的commit
git reset --hard HEAD~1

# 4. 切换到功能分支继续开发
git checkout feature/auth-login
```

---

## 📊 Git提交检查清单

在执行T9阶段时，请使用此清单确保Git提交正确：

```markdown
### Git提交前检查

- [ ] 所有质量门禁已通过（T5-T8.5）
- [ ] 工作区状态已检查（git status）
- [ ] 确认所有修改都是本任务相关的
- [ ] 确认没有临时文件或敏感信息
- [ ] Commit message格式正确
- [ ] Commit message包含任务ID

### Git提交执行

- [ ] 已执行 git add
- [ ] 已执行 git commit
- [ ] 已验证提交成功（git log -1）
- [ ] 工作区已清空（git status显示clean）

### Git推送执行（可选）

- [ ] 已执行 git push
- [ ] 已验证推送成功
- [ ] 远程仓库已同步

### 文档归档

- [ ] 看板已更新（T9列标记为✅）
- [ ] Git信息已记录（commit hash）
- [ ] 文档已归档到tasks目录
- [ ] 任务README已创建
```

---

## 💡 最佳实践

### 1. 提交频率

- ✅ **每个原子任务提交一次**（推荐）
- ✅ 任务完成并通过所有验收后再提交
- ❌ 不要在任务未完成时提交
- ❌ 不要把多个任务的修改合并到一个commit

### 2. Commit Message

- ✅ 使用规范的格式
- ✅ 简洁明了，50字以内
- ✅ 包含任务ID
- ❌ 不要使用模糊的描述（如"update"、"fix bug"）

### 3. 提交内容

- ✅ 只提交本任务相关的文件
- ✅ 确保代码可以正常运行
- ✅ 确保测试通过
- ❌ 不要提交临时文件、日志文件
- ❌ 不要提交node_modules、dist等构建产物
- ❌ 不要提交敏感信息（密码、密钥）

### 4. 分支管理

- ✅ 小任务可以直接在main分支开发
- ✅ 大功能建议使用功能分支
- ✅ 功能分支命名规范：feature/模块名-功能名
- ✅ 定期合并main分支到功能分支

### 5. 推送策略

- ✅ 本地开发可以不推送
- ✅ 重要节点（完成一个模块）推送
- ✅ 每天结束前推送
- ✅ 团队协作必须推送

---

## 🔧 Git配置建议

### 设置用户信息

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### 设置默认编辑器

```bash
# 使用vim
git config --global core.editor vim

# 使用vscode
git config --global core.editor "code --wait"
```

### 设置别名（提高效率）

```bash
# 常用命令别名
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.last 'log -1 HEAD'
git config --global alias.unstage 'reset HEAD --'

# 使用示例
git st  # 等同于 git status
git last  # 查看最后一次commit
```

### 设置.gitignore

在项目根目录创建`.gitignore`文件：

```gitignore
# 依赖
node_modules/
dist/
build/

# 日志
*.log
logs/

# 环境变量
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# 操作系统
.DS_Store
Thumbs.db

# 临时文件
*.tmp
*.temp
.cache/
```

---

## 📚 参考资料

- [Git官方文档](https://git-scm.com/doc)
- [Conventional Commits规范](https://www.conventionalcommits.org/)
- [Git最佳实践](https://github.com/git-tips/tips)

---

**文档结束**

💡 **关键要点**:
1. 🔴 T9阶段必须执行Git提交
2. 🔴 提交前必须通过所有质量门禁
3. 🔴 使用规范的commit message格式
4. 🔴 验证提交成功后再更新看板


