# P-5: 计划列表 UI - T6 变更清单

> **变更时间**: 2026-02-03
> **版本**: v1.1 (补全逻辑与对齐样式)

## 1. 新增组件
- `fit_cycle_web/src/components/plan/CreateOptionsModal.vue`: 实现原型中的“添加计划”二级选项弹窗。
- `fit_cycle_web/src/custom-tab-bar/index.vue`: (H5 专用) 还原原生 SVG 导航栏样式。

## 2. 代码修改记录

### 2.1 页面逻辑 (`Plan/index.vue`)
- [x] 引入 `planService` 并替换本地存储逻辑。
- [x] 修复 `activeTab` 重命名为 `currentTab` 并修正 Vue Props 传递。
- [x] 集成 `CreateOptionsModal`，重写 `createNewPlan` 路由跳转逻辑。
- [x] 隐藏 `PlanStats` 和悬浮按钮以对齐原型。

### 2.2 样式对齐 (`PlanCard.vue` & `PlanHeader.vue`)
- [x] **进度条**: 移除 SVG 环形进度条，实现 Flex 布局线性进度条。
- [x] **Header**: Subtitle 文案修改，调整 Action 按钮颜色与文字显示。
- [x] **全局**: 注入 `Noto Sans SC` 字体并设置为默认。

## 3. 配置文件变更
- `app.config.ts`: 将 `tabBar.custom` 暂时保持为 `false` (或视环境切换) 以适配 H5 截图需求。

## 4. 交付产物验证
- `npm run build:weapp`: 编译通过。
- `audit_p5_ui.js`: 页面加载与数据渲染正常。
