# T6 变更清单 (U-1)

## 新建文件
- `fit_cycle_app/src/dtos/auth-response.dto.ts`: 定义登录响应数据结构，包含 `accessToken`、`refreshToken` 和 `user` 信息。

## 修改文件
- `fit_cycle_app/src/modules/auth/auth.controller.ts`: 
    - 重命名 `login` 方法为 `wechatLogin` 以匹配业务语义。
    - 更新 API 路径为 `wechatAuth` 以匹配现有前端调用。
    - 添加 Swagger 装饰器 (`@ApiOperation`, `@ApiResponse`, `@ApiTags`) 以支持文档自动生成。
    - 将返回类型明确为 `AuthResponseDto`。
- `docs/pj_docs/04_接口与数据规约.md`: 更新 U-1 接口路径为 `/auth/wechatAuth`，请求体为 `WechatAuthDto`，并修正示例。

## 环境变量
- 无新增环境变量。

## 依赖包
- 无新增依赖包。
