# F-1, F-2, F-3: 食物查询与详情接口 - T6 变更清单

## 6.1 修改文件

- `fit_cycle_app/src/modules/food/food.service.ts`:
    - 新增 `findById(id: number)`: 支持通过 ID 获取单一食物的完整营养信息。
    - 确立了 `findFood` 方法作为列表 (F-1) 和 搜索 (F-2) 的统一实现点。
- `fit_cycle_app/src/modules/food/food.controller.ts`:
    - 新增 `GET /food/:id`: 路由映射至 `findById`。
    - 强化了对 `id` 参数的类型校验 (`ParseIntPipe`)。

## 6.2 完整性检查结果

### 【接口功能检查】
- [x] 分页列表功能可用？ (已验证 `findFood` 中的 `applyPagination`)
- [x] 模糊搜索功能可用？ (已验证 `food.name LIKE :name`)
- [x] 详情获取功能可用？ (已新增 `findById`)

### 【模块注册检查】
- [x] 路由是否已正确注册？ (已验证)

### 【构建验证】
- [x] `npm run build` 成功。
