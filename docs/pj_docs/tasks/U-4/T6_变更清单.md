# U-4: 登录按钮组件 - T6 变更清单

## 6.1 新建文件

- `fit_cycle_web/src/components/common/BaseButton.vue`: 
    - 实现了通用的原子按钮组件。
    - 支持 `type` (primary, secondary, wechat, danger, ghost), `size` (sm, md, lg), `loading`, `disabled`, `block`, `round` 等属性。
    - 统一集成了点击缩放反馈和加载中状态。

## 6.2 修改文件

- `fit_cycle_web/src/components/login/LoginButton.vue`:
    - 删除了硬编码的按钮样式。
    - 使用 `BaseButton` 重新实现“微信一键登录”和“先体验一下”按钮。
    - 通过插槽传递微信 SVG 图标。

## 6.3 完整性检查结果

### 【导入语句检查】
- [x] 所有导入的模块都已安装？ (Tailwind CSS 等)
- [x] 所有导入的文件路径都正确？ (`BaseButton` 在 `LoginButton` 中正确引入)
- [x] 所有导入的类/函数都存在？

### 【类型定义检查】
- [x] DTO/Props 定义完整？ (使用了 TypeScript 接口定义 Props)
- [x] 类型一致性检查？ (符合 Vue 3 Composition API 规范)

### 【组件注册检查】
- [x] 新的组件是否可在父组件使用？ (已验证)

### 【构建验证】
- [x] `npm run build:weapp` 通过。
