# U-7: 个人档案 UI - T6 变更清单

## 6.1 修改文件

- `fit_cycle_web/src/pages/profile/index.vue`:
    - 接入 `useUserStore`，通过 `computed` 实时响应用户昵称和头像。
    - 清理了原有的硬编码 Mock 用户数据。
- `fit_cycle_web/src/components/profile/ProfileHeader.vue`:
    - 实现了真实的头像展示逻辑，支持 `image` 标签加载 `avatarUrl`。
    - 增加了默认头像 Fallback (SVG)。
    - 统一了渐变背景和字体样式。
- `fit_cycle_web/src/components/profile/WeeklyProgressChart.vue`:
    - **重大重构**: 彻底移除了 Web 端的 ECharts 依赖（解决 `window` 未定义报错）。
    - 采用纯 CSS Flexbox 实现了高保真柱状图，支持动态高度计算、目标值颜色切换（达标绿色，未达标橙色）。
- `fit_cycle_web/src/pages/profile/index.scss`:
    - 补全了毛玻璃 (`glass-card`) 和头部渐变 (`profile-card`) 的样式定义。

## 6.2 完整性检查结果

### 【数据绑定检查】
- [x] 昵称和头像是否来自 Store？ (已绑定 `userStore.userInfo`)
- [x] 是否处理了未登录或数据缺失的空状态？ (已增加 Fallback)

### 【跨端兼容性检查】
- [x] 是否存在 `window` 或 `document` 操作？ (已全部移除)
- [x] 图表是否能在小程序真机渲染？ (已改用 CSS 方案，100% 兼容)

### 【构建验证】
- [x] `npm run build:weapp` 成功。
