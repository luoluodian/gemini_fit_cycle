# U-6: 用户信息接口 - T6 变更清单

## 6.1 修改文件

- `fit_cycle_app/src/modules/user/user.controller.ts`:
    - 修改控制层路径：`@Controller('users')` -> `@Controller('user')`。
    - 统一接口路径：`me` -> `info`。
    - 确立了 `GET /user/info` 和 `PUT /user/info` 两个标准契约接口。
- `fit_cycle_app/src/modules/user/user.module.ts`:
    - **修复**: 补全了之前缺失的 `controllers: [UserController]` 声明，使得接口正式对外暴露。

## 6.2 完整性检查结果

### 【路由映射检查】
- [x] 后端 `GET /user/info` 与前端 `getUserInfo` 契约匹配。
- [x] 后端 `PUT /user/info` 与前端 `updateUserInfo` 契约匹配。

### 【模块注册检查】
- [x] `UserController` 已注册到 `UserModule`。
- [x] `UserModule` 已导入到 `AppModule` (已验证)。

### 【权限策略检查】
- [x] `JwtAuthGuard` 已正确应用，确保了 UID 的安全提取。

### 【构建验证】
- [x] `npm run build` 成功。
