# U-8: 档案展示组件 - T6 变更清单

## 6.1 新建文件

- `fit_cycle_web/src/components/common/MenuItem.vue`: 
    - 实现了通用的菜单列表项组件。
    - 封装了图标插槽、主标题、子描述、右侧箭头。
    - 统一了点击反馈效果（背景变色 + 微位移）。

## 6.2 修改文件

- `fit_cycle_web/src/components/profile/HealthTools.vue`:
    - 接入 `MenuItem` 替代硬编码布局。
    - 使用 `GlassCard` 统一容器样式。
- `fit_cycle_web/src/components/profile/SettingsMenu.vue`:
    - 接入 `MenuItem` 替代硬编码布局。
    - 使用 `GlassCard` 统一容器样式。
- `fit_cycle_web/src/components/profile/QuickStats.vue`:
    - 使用 `GlassCard` 重构统计卡片，移除了组件内冗余的 CSS。
- `fit_cycle_web/src/components/profile/CurrentPlanCard.vue`:
    - 接入 `BaseButton` 处理计划操作按钮。
    - 统一了文字颜色与透明度。
- `fit_cycle_web/src/components/profile/ImportPlanCard.vue`:
    - 接入 `BaseButton` 处理导入操作。
    - 使用 `GlassCard` 统一容器样式。

## 6.3 完整性检查结果

### 【组件原子化检查】
- [x] 重复的 `menu-item` 样式是否已提取？ (已提取至 `MenuItem.vue`)
- [x] 重复的 `glass-card` 容器是否已统一？ (已统一使用 `GlassCard.vue`)

### 【依赖集成检查】
- [x] 是否全量接入 `BaseButton`？ (已验证所有 Action 按钮均已替换)

### 【构建验证】
- [x] `npm run build:weapp` 成功。
