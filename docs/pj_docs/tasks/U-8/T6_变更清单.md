# U-8: 档案展示组件 - T6 变更清单 (优化)

## 6.1 修改文件

- `fit_cycle_web/src/components/common/BaseButton.vue`:
    - 优化了 `secondary` 和 `ghost` 类型的样式，使其更符合扁平化设计。
    - 移除了冗余的边框，统一了阴影效果。
- `fit_cycle_web/src/components/login/LoginButton.vue`:
    - 将“先体验一下”按钮调整为 `ghost` 类型，降低视觉权重。
    - 统一按钮大小为 `lg`，并增加垂直间距，布局更协调。
- `fit_cycle_web/src/components/login/PrivacyModal.vue`:
    - **滑动修复**: 为 `scroll-view` 设置了固定高度 (`50vh`)，解决了内容无法滑动的问题。
    - **圆角修复**: 使用 `rounded-[32rpx]` 明确定义了弹窗容器的圆角。
    - **关闭按钮优化**: 增大了点击热区，调整了 SVG 图标位置和大小，提升了交互体验。
    - **排版增强**: 优化了分级标题样式，增加了左侧指示条，接入了 `BaseButton` 统一动作按钮。
    - **Bug 修复**: 移除了重复的 `<script setup>` 块，解决了构建错误。

## 6.2 完整性检查结果

### 【交互反馈检查】
- [x] 按钮点击是否有缩放反馈？ (已集成在 `BaseButton`)
- [x] 隐私政策是否可滑动？ (已通过固定高度修复)
- [x] 关闭按钮是否易于点击？ (已增大热区)

### 【构建验证】
- [x] `npm run build:weapp` 成功。