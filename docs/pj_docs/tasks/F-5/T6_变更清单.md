# F-5: 种子数据填充 - T6 变更清单

## 6.1 新建文件

- `fit_cycle_app/src/database/migrations/seed-food.sql`: 
    - 包含了 22 种高质量核心食材的 SQL 插入脚本。
    - 涵盖蛋白质、优质碳水、常见蔬果、油脂及乳制品。
    - 所有数据均按照 100g/100ml 标准营养成分进行精确换算。

## 6.2 数据库变更

- **操作**: 执行了 `seed-food.sql`。
- **结果**: 
  - 成功向 `foods` 表注入了 22 条记录。
  - 数据完全符合 F-4 任务设计的 `DECIMAL(8,2)` 精度标准。
  - 完善了 `description` 和 `gi` (升糖指数) 字段。

## 6.3 完整性检查结果

### 【数据一致性检查】
- [x] 所有热量计算近似符合 `4-9-4` 原则。
- [x] 关键字段 (id, name, calories_per_100g) 无缺失。
- [x] 分类字段 (`protein`, `carbs`, `vegetables` 等) 与前端过滤逻辑匹配。

### 【验证】
- [x] `mysql` 命令行验证总记录数为 22 条。
