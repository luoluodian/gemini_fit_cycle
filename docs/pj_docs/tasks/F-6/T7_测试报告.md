# F-6: 新增食物接口 - T7 测试报告

## 7.1 验证环境
- **工具**: Manual REST API Verification (Postman/Curl)
- **范围**: 食物增删改查全链路验证

## 7.2 验证结果

### 7.2.1 新增食物 (POST)
- **测试项**: 调用 `POST /food` 创建“测试西兰花”。
- **结果**: ✅ **PASS**
- **验证**: 数据库成功生成记录，`caloriesPer100g` 等数值字段入库正常。

### 7.2.2 重复项拦截
- **测试项**: 再次插入同名食物。
- **结果**: ✅ **PASS**
- **预期**: 返回 `{ error: '该食物已存在' }`。

### 7.2.3 列表查询与过滤
- **测试项**: 访问 `GET /food?minCalorie=100`。
- **结果**: ✅ **PASS**
- **验证**: SQL 执行正常，过滤逻辑正确引用了 `calories_per_100g` 列。

### 7.2.4 字段映射一致性
- **测试项**: 检查更新接口 `PUT /food/:id`。
- **结果**: ✅ **PASS**
- **验证**: 局部更新字段（如仅更新 `description`）功能正常。

## 7.3 验收结论
新增食物接口已按最新表结构完成标准化重构，数据流转闭环，满足交付要求。
