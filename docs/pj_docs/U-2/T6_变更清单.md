# U-2: 认证服务封装 - T6 变更清单

> **变更日期**: 2026-01-31 (补录)  
> **执行人**: AI Assistant

---

## 1. 变更摘要
在前端封装了 `AuthService`，统一处理登录请求、Token 存储及类型定义。

## 2. 文件变更列表
| 文件路径 | 变更类型 | 简要说明 |
|:---|:---|:---|
| `fit_cycle_web/src/services/modules/auth.ts` | 新增 | 封装 `login` 方法及 `UserInfo` 接口。 |

## 3. 详细变更内容
- [x] 定义 `UserInfo` 接口，对齐后端 DTO。
- [x] 封装 `login` 函数，调用 `/auth/wechatAuth`。

## 4. 验证计划
1.  **编译验证**: `npm run build:weapp`。
